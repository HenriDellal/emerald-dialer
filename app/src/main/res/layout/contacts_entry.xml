<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
	android:layout_width="match_parent"
	android:layout_height="wrap_content"
	android:padding="8dp">
	<ImageView
		android:id="@+id/contact_entry_image"
		android:layout_width="48dp"
		android:layout_height="48dp"
		android:layout_alignParentStart="true"
		android:layout_alignParentTop="true"
	/>
	<TextView
		style="?attr/contactNameStyle"
		android:id="@+id/contact_entry_name"
		android:layout_toEndOf="@id/contact_entry_image"
		android:layout_alignTop="@id/contact_entry_image"
		android:layout_marginStart="8dp"
		android:textSize="16sp"
	/>
	<TextView
		style="?attr/additionalTextStyle"
		android:id="@+id/contact_phone_number"
		android:layout_alignBottom="@id/contact_entry_image"
		android:layout_alignStart="@id/contact_entry_name"
		android:textSize="12sp"
	/>
	<TextView
		style="?attr/additionalTextStyle"
		android:id="@+id/contact_entry_number_type"
		android:layout_alignParentBottom="true"
		android:layout_alignParentEnd="true"
		android:textSize="12sp"
		/>
</RelativeLayout>
